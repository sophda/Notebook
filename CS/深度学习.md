# 深度学习

## 打标签

1. 打开matlab，新建main.m脚本

   ```matlab
   clc;
   clear;
   % videoname 写文件名 不带后缀，不带中文，可用自己名字的缩写代替 
   videoname = "video"
   
   obj = VideoReader(videoname+".mp4");%输入视频位置
   numFrames = obj.NumberOfFrames;% 帧的总数
   i=0
    for k = 1 :20:numFrames
        i=i+1
        frame = read(obj,k);%读取第几帧
       % imshow(frame);%显示帧
         imwrite(frame,strcat(videoname+"_"+num2str(i),'.jpg'),'jpg');% 保存帧
    end
   
   %%
   mkdir("data")
   mkdir("data/imgs")
   mkdir("data/masks")
   load('gTruth.mat')
   namelist = dir("./PixelLabelData/*.png")
   for i = 1:length(namelist)
       [~,name,~] = fileparts(namelist(i).name);
       id = name(strlength("Label_")+1:end)
       labelname = ["./PixelLabelData/Label_"+id+".png"];
       imgname = [videoname+"_"+id+".jpg"]
       copyfile(imgname,"./data/imgs/"+imgname);
       copyfile(labelname,"./data/masks/"+videoname+"_"+id+".png")
       
   end
   ```

2. 将自己的视频文件修改成**简洁**的名字，比如**lsp**，并同步修改脚本中的videoname

3. 运行脚本的第一段，会在同级目录下生成系列的帧

4. 在 app 中打开**image labeler**

   ![image-20231231194055579](src/image-20231231194055579.png)

5. 选择从文件中加载 图片，及刚才截取的帧

   ![image-20231231194223795](src/image-20231231194223795.png)

   导入后，界面如下：

   ![image-20231231194302819](src/image-20231231194302819.png)

6. 点击label，会出现**define new roi label**选项框，然后依次填写label name，pixlabel

   - label name填写**yuan**，也就是圆形的血管
   - pixel label 表示像素填充

   ![image-20231231194556949](src/image-20231231194556949.png)

   点击确定，此时会出现：**yuan**的标签

   ![image-20231231194745231](src/image-20231231194745231.png)

   再新建一个名为 **chang**的标签，同样选择**pixlabel**：

   ![image-20231231194914909](src/image-20231231194914909.png)

7. 打标签

   **如果什么血管都没有，直接跳过这个图片**

   - 如果是圆形视角血管，选择**yuan**这个标签，然后点击**Brush**，调整合适大小进行涂抹

     ![image-20231231195044823](src/image-20231231195044823.png)

   - 如果是长方形视角的血管，选择**chang**这个标签，然后涂抹

     ![image-20231231195152415](src/image-20231231195152415.png)

8. 导出，选择导出到文件

   ![image-20231231195237219](src/image-20231231195237219.png)

9. 运行脚本文件中的第二段，会新建一个**data**文件夹，然后把相关的图片复制进去，效果如下：

   ![image-20231231195430428](src/image-20231231195430428.png)

**可以用上下左右箭头快速切换图片，及时保存当前session，以免数据丢失**



## anaconda

1.创建虚拟环境

```
conda create -n pytorch python=3.7
```

2.删除

```
conda remove -n pytorch -all
```

## pytorch安装

1. 笑死，使用conda竟然跟我说找不到？？
2. 用pip安装，不用挂梯子，用pytorch原网站安装，带宽跑满了好吧？！狗吧conda

## python基础

```
x = isinstance(5, int)
```

判断某个变量是不是某个类型的

![image-20240618110016647](src/image-20240618110016647.png)

## numpy基础（一）

**1.1**

```python
A = np.array()
B = np.array()

    (1)A*B  等价于  np.multipy(A,B)
即对应位置相乘
    (2)推广：激活函数
x = np.random.rand(2,3)  # x的形状
def softmoid(x):
    return 1/(1+np.exp(-x))
def relu(x):
    return np.maxinum(0,x)
def softmax(x):
    return np.exp(x)/np.sum(np.exp(x))
```

**1.2 点积**

即为矩阵相乘

语法：result = np.dot(a,b)

**1.3 格式**

```python
import torch
import numpy as np
import cv2
img=cv2.imread('1.jpeg')
print(type(img))
输出的图片格式为 array格式
```

**列表与ndarry格式：**

![](src/2023-02-14-02-38-45-image.png)

**2 数组变形**

```python
arr.reshape()
改变维度
```

```python
arr.resize()
改变维度，改变向量自身
```

```python
arr.travel
对向量进行展平，不会产生原数组的副本
```

```python
arr.flatten
展平，产生副本
```

```python
arr.squeeze
 对维数为1的维度进行降维。对多维数组使用时无作用
```

```python
arr.transpose 
对高位矩阵进行轴对换
```

**3 数组合并**

```python
np.append
np.concatenate
np.stack
np.hstack
np.vstack
np.dstack
np.vsplit
```

（1）说明：append，concatenate，stack都有axis参数，用于控制数组的合并方式时按行还是按列

（2）对于append和concatenate，待合并的数组必须由相同的行数或列数

（3）stack，hstack，dstack 要求待合并的数组必须具有相同的形状

**4 批量操作**

```python
'''
批量处理
'''
data_train=np.random.randn(10000,2,3)
print(data_train.shape)
np.random.shuffle(data_train)
batch_size=100
foriinrange(0,len(data_train),batch_size):
    x=np.sum(data_train[i:i+batch_size])
print("batch{},sum{}".format(i,x))
```



### 5.一些函数

1. **.max**

   ```
   a = np.array([[1,2,4],[4,5,6],[7,8,9]])
   print(a.max(1))
   ```

   $$
   \begin {bmatrix}
   1 & 2 & 4 \\
   4 & 5 & 6 \\
   7 & 8 & 9
   \end {bmatrix}
   $$

   - 即第一维度，横着的 4 6 9
   - 第0维度，竖着的 7 8 9





## Numpy基础（二）

![](src/2023-02-14-02-46-24-image.png)

b是一个位置型数组，每一个值是bool型的，b中为1的位置将a中对应位置修改为指定值

## tensor

1. 概述

2. ndarray 转换为 Tensor

```python
a = np.array()

a1 = torch.from_numpy(a)
```

3. 逐元素操作

```python
abs/add

addcdiv

sigmoid/tanh/softmax
```

## 损失函数

**1. 交叉熵损失**

nn.cross_entropy()

该损失函数已经将softmax，sigmoid，传统交叉熵集成，因此训练时只需要将神经元输出直接放入即可

## 数据

1. 数据增强方法
  
   如flip、rotate、random、move & crop、GAN

![](src/2023-02-14-02-49-51-image.png)

2. 数据集

```python
from torch.utils.data import DataLoader
// DataLoader方便batchsize操作


from torch.utils.data import Dataset


class dataset(Dataset):

    def __init__(self):
        super
```

## 训练

```python
device = torch.device('cuda')


model = model().to(device)

criten = nn.CrossEntropyLoss().to(device)

optimizer = optim.Adam(model.parameters, lr = 1e-3)



for epoch in range(epoches):

    model.train()

    for batch_id,(x,label) in enumuerate (train):

        x,label=x.to(device), label.to(device)

        logits = model(x)  //调用forward函数

        loss = criten(logits, label)  // Tensor Scale

        optimizer.zero_grad() //优化器梯度归零，防止梯度累加
        loss.backward()  //反向传播
        optimizer.step() 
         //优化器优化，对优化器参数 model.Parameters()即模型参数优化，使模型收敛

    model.eval()
    with torch.no_grad():
        for x,label in data_test:
...
```

**使用GPU:**

```python
device = torch.device("cuda")
module.to(device)
input.to(device)
target.to(device)
loss_func.to(device)
```

![](src/2023-02-14-02-58-18-image.png)

## 蒙特卡洛链MCMC



## 稀疏正则化

**正则化方法：**

L0范数是指向量中非0的元素的个数。(L0范数很难优化求解)

L1范数是指向量中各个元素绝对值之和

L2范数是指向量各元素的平方和然后求平方根

**为什么在loss函数中增加l1范数会给出稀疏解？**

![img](src/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3UwMTE0MjYwMTY=,size_16,color_FFFFFF,t_70#pic_center.png)

l1范数是红色的（表征各个绝对值之和），蓝色线表示要拟合的线，在loss包含l1范数时，红色的线会逼近蓝色的线，从而在坐标轴上产生**稀疏解**，如[1000,0.5]这种

## latent space（隐式空间）

- 隐空间只是一种压缩数据的表示，其中相似的数据点在空间上更接近。
- 隐空间对于学习数据特征、寻找更简单的数据表征从而进行分析很有帮助。
- 我们可以通过分析隐空间中的数据来了解数据点之间的模式或结构相似性，无论是通过流形（manifolds）还是聚类等方式。
- 我们可以在隐空间中插值数据，并使用我们模型的解码器来“生成”数据样本。
- 我们可以使用t-SNE和LLE等算法将隐空间可视化，这些算法将我们的隐空间表征转化为二维或三维。

在深度学习中,隐式空间(Latent Space)是指一种低维度的连续向量空间,用于对原始高维输入数据(如图像、文本等)进行编码和解码。隐式空间旨在捕获输入数据的关键语义信息,同时降低了表示的维度和复杂性。

隐式空间通常由神经网络自动学习得到,而不是由人为设计。一些常见的利用隐式空间的深度学习模型包括:

1. 自编码器(Autoencoder):自编码器由编码器(encoder)和解码器(decoder)两部分组成。编码器将高维输入数据映射到低维隐式空间,解码器则将隐式空间的向量还原为原始输入数据的重建。

2. 变分自编码器(Variational Autoencoder):在自编码器的基础上,引入了隐变量的概率分布约束,可以更好地捕获数据的潜在分布。

3. 生成对抗网络(Generative Adversarial Networks):通过生成器(generator)和判别器(discriminator)的对抗训练,生成器学习从隐式空间中采样并生成逼真的样本数据。

隐式空间允许深度学习模型更紧凑地表示数据,并且可以对隐变量空间进行有意义的操作和插值。例如在图像领域,我们可以在隐式空间中平滑地改变隐变量的值,实现图像风格和属性的平滑过渡。因此,隐式空间对于许多任务(如降噪、插值、数据压缩等)非常有用。



# Pytorch API

## clip_grad_value_()

torch.nn.utils.clip_grad_value_

在 PyTorch 中，`torch.nn.utils.clip_grad_value_` 函数用于在训练神经网络模型期间将参数的梯度裁剪到指定的范围。这样做有助于防止梯度爆炸问题，即梯度可能变得过大，导致训练过程不稳定。

您提供的示例中：

```python
torch.nn.utils.clip_grad_value_(policy_net.parameters(), 100)
```

这行代码将梯度裁剪应用于名为 `policy_net` 的模型的参数。`policy_net` 中所有参数的梯度将被裁剪，以确保它们的值限制在 `[-100, 100]` 范围内。各部分的含义如下：

- `policy_net.parameters()`: 这会获取 `policy_net` 模型所有参数的迭代器，通常是权重和偏置。

- `100`: 这是裁剪值。意味着每个参数的梯度将被裁剪，使其不小于 `-100` 且不大于 `100`。

这种裁剪是就地完成的，即它直接修改模型参数的梯度，无需返回和重新赋值参数。这种方法通常用于强化学习以及预期在训练中会出现大梯度的场景。



## unsqueeze() & squeeze()

1. unsqueeze()函数起升维的作用,参数表示在哪个地方加一个维度。
   1在第一个维度(中括号)的每个元素加中括号
   0表示在张量最外层加一个中括号变成第一维。

   可以参考transpose()的解释，比如说a是一维的，那么维度`a.shape = [6]`，那么unsqueeze（1）表示在2维度上增加一维，也就是维度变成了[6 1]，即6行1列。

   > 其他的同理，比如说[2 2]维度的执行了unsqueeze(2),变换之后的维度变为[2 2 1]

   ![image-20240605213526974](src/image-20240605213526974.png)

   2. squeeze（）

      表示压缩维度，d变量先是在c的基础上进行升维度，由[2，2]升维到[2，2，1]

      然后执行降维，因为d是[2,2,1]，所以如果是降得0，1维度，那么对d没有影响；如果降维的是2维，则变成[2，2]维
      
      ![image-20240618112102910](src/image-20240618112102910.png)
      
      ![image-20240618112008718](src/image-20240618112008718.png)
   
   









## torch.rand()

Returns a tensor filled with random numbers from a uniform distribution on the interval [0,1)

```
torch.rand(4)
tensor([ 0.5204,  0.2503,  0.3525,  0.5673])

torch.rand(2, 3) // 2行3列
tensor([[ 0.8237,  0.5781,  0.6879],
        [ 0.3816,  0.7249,  0.0998]])
        
a = torch.rand(2,3,4)  // 2个batch，3行4列
print(a)
tensor([[[0.7060, 0.9734, 0.8331, 0.2386],
         [0.0197, 0.5055, 0.4368, 0.6771],
         [0.9418, 0.1444, 0.9721, 0.2699]],

        [[0.5333, 0.3716, 0.9089, 0.5091],
         [0.5446, 0.2952, 0.7340, 0.7370],
         [0.7602, 0.7747, 0.6086, 0.7327]]])
```

## view()

tensor.view()

Returns a new tensor with the same data as the tensor but of a different shape.

当维度中有-1是，是**从其他维度推断出来**的。

```0
>>> x = torch.randn(4, 4)
>>> x.size()
torch.Size([4, 4])
>>> y = x.view(16)
>>> y.size()
torch.Size([16])
>>> z = x.view(-1, 8)  # the size -1 is inferred from other dimensions
>>> z.size()
torch.Size([2, 8])

>>> a = torch.randn(1, 2, 3, 4)
>>> a.size()
torch.Size([1, 2, 3, 4])
>>> b = a.transpose(1, 2)  # Swaps 2nd and 3rd dimension
>>> b.size()
torch.Size([1, 3, 2, 4])
>>> c = a.view(1, 3, 2, 4)  # Does not change tensor layout in memory
>>> c.size()
torch.Size([1, 3, 2, 4])
>>> torch.equal(b, c)
False
```

## transpose()

Returns a tensor that is a transposed version of `input`. The given dimensions `dim0` and `dim1` are swapped.

如果是低维度的，比如：这样就是简单的做转置，（0，1）和（1，0）结果相同。表示0维和1维互换。

![image-20240529022009708](src/image-20240529022009708.png)

如果是高维度的，那么直接想象成那两个维度的数值互换，比如$A_{122}=5$,即A中**第1个batch中的第2行第2列**为5，如果进行转置`transpose(0,2)`把0维度和2维度互换，那么新的矩阵中$A_{221}=5$。可以用这种方式进行下面的列子。

![image-20240529023004616](src/image-20240529023004616.png)



## torch.max()

查看最大的尺度

```
torch.max(input,dim)
```



## nn.Linear()

都知道这个是创建的线性层，输入维度->输出维度，但是咋操作的呢？

nn.linear（4，3）实际上是存了个矩阵权重，在乘输入tensor

![image-20241018225626523](src/image-20241018225626523.png)



如果是一个多维度的进入了linear模块：

比如（2，2，4）输入，那么输出变为（2，2，3），也就是只改变了最后一个维度大小。

![image-20241019003759597](src/image-20241019003759597.png)

## *和@

- *是矩阵对应元素相乘
- @是矩阵乘法

## dim=0,1,2,3

比如一个数组：
$$
A_{m*n*p*q}
$$
我们要指定他的维度的话，比如dim=0，那就是只让m变化，其他所有的保持不变，得到的即是这个维度的数据

比如$A_{3*4*5*6}$,我们要对dim=1的数据进行归一化，也就是对每个$A_{3*i*5*6}$共`3*5*6`个向量进行归一化

![image-20241022011955831](src/image-20241022011955831.png)

## 高维矩阵乘法

> 参考知乎的博客：[【全面理解多维矩阵运算】多维（三维四维）矩阵向量运算-超强可视化 - 知乎](https://zhuanlan.zhihu.com/p/337829793)

二维数组已经比较直观，就是行列；那么三维矩阵的话就是二维矩阵的堆叠，四维矩阵又是三维矩阵的堆叠·····

因此一个矩阵的维度比如是：`3*2*4*5`，那么只有最后的`4*5`是关键的数据维度，其他的n-2维度都是后面`4*5`矩阵的不同排列方式而已。

最后做矩阵乘法运算就需要保证：

- **矩阵的前n-2维度需要保持一致**
- 最后的行列要保持对应，如`5*4`的矩阵@`4*5`的矩阵

# Transformer

![image-20240528170114836](src/image-20240528170114836.png)

## 模型自回归

>  模型自回归（Autoregressive model, AR model）是一种**时间序列预测模型**，它通过**使用过去的数据点来预测未来的值**。自回归模型假设当前的观测值可以表示为过去观测值的线性组合，并且通常还包含一个随机误差项。
>
> 具体来说，自回归模型的形式可以表示为：
> $$
> y_t = c + \phi_1 y_{t-1} + \phi_2 y_{t-2} + \ldots + \phi_p y_{t-p} + \epsilon_t
> $$
> 其中：
>
> - \( y_t \) 是时间 \( t \) 的观测值。
> - \( c \) 是常数项。
> - \( \phi_1, \phi_2, \ldots, \phi_p \) 是模型的参数，它们表示每个滞后值的系数。
> - \( \epsilon_t \) 是随机误差项，通常假设其均值为零且方差恒定（即白噪声）。
> - \( p \) 是模型的阶数，表示使用多少个过去的观测值来进行预测。
>
> 例如，一个自回归模型的阶数为 1（即 AR(1) 模型），可以写作：
>
> $ y_t = c + \phi_1 y_{t-1} + \epsilon_t $
>
> 自回归模型在时间序列分析中非常常用，因为它们能够捕捉数据中的依赖结构和趋势，适用于很多实际场景，例如经济数据分析、气象数据预测等。
>
> 总结来说，自回归模型通过利用时间序列自身的历史值来进行预测，是时间序列分析中的一种重要工具。

在Transformer中，在每一步，模型都是自回归的，在生成下一个符号时，消耗先前生成的符号作为额外的输入。



## 编码-解码结构

编码器将符号表示的输入序列( x1 , ... , xn)映射到连续表示的序列z = ( z1 , ... , zn)。给定z，解码器然后生成一个输出序列( y1 , ... , ym)的符号一次一个元素。

### Encoder

编码器由N = 6个相同层的堆栈组成。每层有两个子层。第一种是多头自注意力机制，第二种是简单的、基于位置的全连接前馈网络

我们在两个子层的每一个周围使用一个残差连接，然后进行层规范化。也就是说，每个子层的输出是LayerNorm( x +子层( x) )，其中子层( x )是子层本身实现的函数。为了便于这些残差连接，模型中的所有子层以及嵌入层都会产生维数dmodel = 512的输出。

### Decoder

解码器：解码器也是由N = 6个相同层的堆栈组成。除了每个编码器层中的两个子层之外，解码器还插入第三个子层，它对**编码器堆栈的输出**执行多头注意力。与编码器类似，我们在每个子层周围使用残差连接，然后进行层归一化。我们还修改解码器堆栈中的自注意力子层，以防止位置出现在后续位置。这种掩蔽，结合输出嵌入被一个位置偏移的事实，确保了对位置i的预测只能依赖于位置小于i的已知输出。



## Attention机制

注意力函数可以描述为将一个查询和一组键值对映射到一个输出，其中查询（query）、键（keys）、值（values）和输出（outputs）都是向量。输出计算为值的加权和，其中分配给每个值的权重由查询与相应键的兼容函数计算。

### Scaled Dot-Product Attention（缩放点积注意力）

![image-20240528171035584](src/image-20240528171035584.png)

**缩放点积注意力**所执行的为：

![image-20240528172123086](src/image-20240528172123086.png)

也就是将查询query与键做点积，然后进行缩放$\sqrt{d_k}$ ，最后与值value相乘

> 这个dk表示键向量的维度，进行缩放的目的是:为了在计算点积时避免因键向量长度较大而导致的梯度消失或爆炸问题，从而保证训练的稳定性。

>加性注意力使用具有单隐层的前馈网络计算相容性函数。虽然两者在理论复杂度上相似，但点积注意力在实际应用中速度更快，空间效率更高，因为它可以使用高度优化的矩阵乘法代码实现.
>
>而对于较小的dk值，两种机制的表现类似，对于较大的dk值，相加注意力优于不缩放的点积注意力[ 3 ]。我们猜想，对于dk的大值，点积的大小会变大，从而将softmax函数推到梯度极小的区域。为了抵消这种影响，我们对点积进行1√dk的缩放。



### Multi-Head Attention (多头注意力机制)

>多头注意力允许模型在不同位置共同关注来自不同表示子空间的信息。在单个注意头的情况下，平均抑制了这一点。

多头注意力机制首先对query、key、value进行线性投影/

![image-20240528173831861](src/image-20240528173831861.png)





# Large Language Model

## 概述

大模型可以分为两步：预训练与微调

- 预训练是训练模型具有预测下一个词的能力
- 微调是增强大模型在某方面的能力

## 文本数据编码

要将输入的文本输入到模型，需要经过：分词--给分的词一个id--将id转换为词嵌入

- 分词：就是把一个句子分成一个个的词
- tokenized：将词转换为id，一般是int型
- 将词转换为词嵌入：将int型转换为高维度向量（embedding）

![image-20241013215747863](src/image-20241013215747863.png)

![image-20241013222009836](src/image-20241013222009836.png)



## embedding层

>  one hot的概念：就是将数字来代替某个文字。
>
> 这在分类模型中，比如有3类，用[0,0,1]来表示具体的类别，这种方式对于比较少的数量用独热码比较好用。但是数据量大、数据过于稀疏时，会非常占用资源。

![image-20250618192704031](src/image-20250618192704031.png)

---



在llm中，将数据转换为向量格式的概念通常称为嵌入。GPT-3的嵌入尺寸为vocab = 12288.

如果在pytorch中创建一个embedding层：比如将6个词映射为三维，即int->[1.1, 1.2, 1.3]这样子

```
vocab_size = 6
output_dim = 3

embedding_layer = torch.nn.Embedding(vocab_size, output_dim)

print(embedding_layer.weight)
```

此时打印出来的权重矩阵为：

![image-20250618194316594](src/image-20250618194316594.png)

- 行代表token id的总数，比如token中一共有6个词！

- 列表示嵌入词维度，比如这里是三维。

**如果一个词“你”经过tokenizer之后，其token id为3，那么通过embedding层获取到的词嵌入向量为：[-0.4015, 0.9666, -1.1481]**，也就是第3+1行的向量。

因为词->token id->embedding是连贯的，所以不同的embedding层取决于其tokenizer。







## self-attention 注意力

- query：用于查询句子中的其他的部分，用处是探索其他部分
- key：输入的每个单词都会有一个key，与query结合进行查询
- value：表示输入项的表示，模型通过query与key做内积选择哪个key最相关，通过attentionMatrix@values得到contex vectors



![image-20241013225639483](src/image-20241013225639483.png)





## causal attention 因果注意力

保证模型只能看到当前输入之前的内容。作用有：

- 避免信息泄露，只能看到左边的信息
- 时间一致
- 适用于自回归模型，因为在生成任务中，需要根据之前的词来生成下一个词，这样保证模型看不到后面的词

> 假设有一个句子 "The cat sat on the mat."，模型需要逐步生成每个词：
>
> - 生成第一个词 "The" 时，模型没有任何上下文，只能依赖于起始符。
> - 生成第二个词 "cat" 时，模型只能使用 "The" 这个词作为上下文，不能看到 "sat on the mat"。
> - 生成第三个词 "sat" 时，模型只能使用 "The cat"，不能提前知道 "on the mat"。

1.

![image-20241014001909480](src/image-20241014001909480.png)

通过将注意力权重修改成一般，这样q*k得到的注意力权重在去查询value的时候，就只能得到当前词之前的value。

**2.掩码实现**，更有效的方式

- 将对角线上面的值设为-inf
- 使用sigmoid函数激活，由于sigmoid的特性，-inf会得到0的输出，这样就避免了相加在归一化

![image-20241014012353608](src/image-20241014012353608.png)



## MHA

1.

> 主要思想是多次并行运行注意力机制，使用不同的、学习到的投影数据（就是输入数据与权重数据的乘积，比如query与注意力权重相乘）

如下图所示，展示了使用因果注意力机制堆叠而成的“两头”注意力头

与之前的因果注意力不同，这里的输出向量进行了链接

![简化版多头注意力](src/image-20241015004602268.png)

2.implement via python

[高维矩阵乘法](##高维矩阵乘法)

流程总结：

- 输入向量分别于wq、wk、wv矩阵相乘，得到q、k、v
- q@k（点积）得到注意力分数attn_score
- 根据tokens数量，屏蔽attn_score上三角矩阵元素，attn_score经过dropout处理变成attn_weight
- attn_weights @ values得到注意力输出

```python
class MultiHeadAttention(nn.Module):
    def __init__(self,d_in,d_out,context_length,dropout,num_heads,qkv_bias=False):
        super().__init__()
        self.d_out = d_out 
        self.num_heads = num_heads
        
        
        self.head_dim = d_out // num_heads #A 
        self.W_query = nn.Linear(d_in, d_out, bias=qkv_bias) 
        self.W_key = nn.Linear(d_in, d_out, bias=qkv_bias) 
        self.W_value = nn.Linear(d_in, d_out, bias=qkv_bias) 
        self.out_proj = nn.Linear(d_out, d_out) #B 
        self.dropout = nn.Dropout(dropout) 
        self.register_buffer( 'mask', torch.triu(torch.ones(context_length, context_length), diagonal=1) ) 
    def forward(self, x): 
        b, num_tokens, d_in = x.shape 
        
        
        keys = self.W_key(x) #C 
        queries = self.W_query(x) #C 
        values = self.W_value(x) #C 
        # 上面的这三行主要是将输入tensor与qkv三个权重矩阵进行相乘，
        # 由（batch_size, num_tokens, d_in）->（batch_size, num_tokens, d_out）
        
        keys = keys.view(b, num_tokens, self.num_heads, self.head_dim) #D 
        values = values.view(b, num_tokens, self.num_heads, self.head_dim) #D 
        queries = queries.view(b, num_tokens, self.num_heads, self.head_dim)#D 
        # 上面这三行 将乘权重矩阵得到的qkv矩阵view成：
        # batch_size, num_tokens, num_heads, head_dim
        # 跟view之前的相比，主要是将(batch_size, num_tokens, d_out)里的d_out变成了(num_heads, head_dim)
        
        
        keys = keys.transpose(1, 2) #E 
        queries = queries.transpose(1, 2) #E 
        values = values.transpose(1, 2) #E 
        # 这里是维度改变1，2维互换
        # 得到的是： (batch_size, num_heads, num_tokens, head_dim)
        
       
        
        attn_scores = queries @ keys.transpose(2, 3) #F 
        # 执行的是
        #  (batch_size, num_heads, num_tokens, head_dim) @ (batch_size, num_heads, head_dim, num_tokens)
        # 参考上文的高维矩阵乘法，最后做运算的应该是最后两个维度，即(num_tokens, head_dim) @ (head_dim, num_tokens)
        # 这样得到的是(batch_size, num_heads, num_tokens, num_tokens),也就是每个batch中，每个head都有一个注意力分数，共有num_heads个头，如1中的         # 图片显示
        # 
        
        mask_bool = self.mask.bool()[:num_tokens, :num_tokens] #G 
        attn_scores.masked_fill_(mask_bool, -torch.inf) #H 
        attn_weights = torch.softmax( attn_scores / keys.shape[-1]**0.5, dim=-1) 
        attn_weights = self.dropout(attn_weights) 
        # 应用 因果机制，屏蔽上半三角权重
        # 应用dropout，训练时随机丢弃一部分权重，推理时捡起来
        # attn_weights：(batch_size, num_heads, num_tokens, num_tokens)
        
        
        context_vec = (attn_weights @ values).transpose(1, 2) #I #J 
        # attn_weights:        (batch_size, num_heads, num_tokens, num_tokens)
        # values:              (batch_size, num_heads, num_tokens, head_dim)
        # attn_weights@values: (batch_size, num_heads, num_tokens, head_dim)
        # transpose:           (batch_size, num_tokens, num_heads, head_dim)
        
        
        
        context_vec = context_vec.contiguous().view(b, num_tokens, self.d_out) 
        # context_vec: (batch_size, num_tokens, num_heads*head_dim)
        
        
        context_vec = self.out_proj(context_vec) #K 1
        return context_vec
```

> 最后有一个out_porj部分，这在因果注意力中并不是必要的。

至此，已经实现了一个基本的因果注意力，GPT-2 117M有12个注意力头，上下文向量嵌入为768，d_in=d_out

## GPT-like model

1.gpt-like model总体结构

总体流程可以如下图所示：

![image-20241022004559968](src/image-20241022004559968.png)



**2.层归一化**

层归一化主要思想是将神经网络的输出（激活）调整为均值为0、方差为1 .  好处：

- 加快了有效权重的收敛速度
- 保证了一致、可靠的训练

![image-20241022011442938](src/image-20241022011442938.png)



# 图像生成

图像生成领域的模型有GAN 和 VAE模型。

## GAN模型



## diffusion模型

### 基本原理

- 和其他生成模型一样，实现从噪声（采样自简单分布）生成目标数据样本。

- 扩散模型包括两个过程：前向过程（forward process）和反向过程（reverse process），其中前向过程又称为扩散过程（diffusion process）。无论是前向过程还是反向过程都是一个参数化的[马尔可夫链](https://zhida.zhihu.com/search?content_id=226847260&content_type=Article&match_order=1&q=马尔可夫链&zd_token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJ6aGlkYV9zZXJ2ZXIiLCJleHAiOjE3NTc4Mzg1ODksInEiOiLpqazlsJTlj6_lpKvpk74iLCJ6aGlkYV9zb3VyY2UiOiJlbnRpdHkiLCJjb250ZW50X2lkIjoyMjY4NDcyNjAsImNvbnRlbnRfdHlwZSI6IkFydGljbGUiLCJtYXRjaF9vcmRlciI6MSwiemRfdG9rZW4iOm51bGx9.tgSO8T5QdeZAPUubfXdxqUBwO0TRWTHAeZs9jPuG1Js&zhida_source=entity)（Markov chain），其中反向过程可用于生成数据样本（它的作用类似GAN中的生成器，只不过GAN生成器会有维度变化，而DDPM的反向过程没有维度变化）。



### 前向过程

前向过程是加噪声的过程，前向过程中的图像，$x_t$只和上一时刻的图像$x_{t-1}$有关，该过程可以视为马尔可夫过程，满足：

![image-20250912170135691](src/image-20250912170135691.png)



### 反向过程

![image-20250912171511226](src/image-20250912171511226.png)



### 网络结构

1. unet，整个训练过程其实是训练unet网络的参数

   unet的职责是根据当前的样本和时间t预测噪声，无论是在前向还是反向过程中
